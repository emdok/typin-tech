<div class="comments">
  <h2 class="mb-3">Comments</h2>
  {{#each this}}
  <section class="comment mb-3">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>