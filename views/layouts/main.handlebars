<!DOCTYPE html>
<html lang="en" class="h-100">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Typin Tech</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Work+Sans:wght@200;300;500;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="stylesheet" href="/stylesheets/main.css">
</head>

<body class="bg-dark text-white d-flex h-100">
  <div class="container d-flex w-100 h-100 p-3 mx-auto flex-column">
    <div class="mb-auto">
      <header class="container">
        <nav class="nav nav-masthead justify-content-center float-md-end">
          <a class="nav-link text-white" href="/dashboard">Dashboard</a>
          <a class="nav-link text-white" href="/">Home</a>
          {{#if loggedIn}}
          <button id="logout" class="nav-link btn btn-link text-white">Logout</button>
          {{else}}
          <a class="nav-link text-white" href="/login">Login</a>
          {{/if}}
        </nav>
        <h1 class="row">
          <a class="text-white site-title" href="/">Typin' Tech</a>
        </h1>
      </header>
    </div>

    <div class="px-3">
      <main>
        {{{ body }}}
      </main>
    </div>
    <footer class="mt-auto text-white-50">
      <p>
        <a class="text-white" href="github.com/emdok/typing-tech">Created by Emdok</a>
      </p>
    </footer>

  </div>

  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>